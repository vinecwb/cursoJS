const usuario = {
    email: 'aluno@cod3r.com.br',
};

usuario.nome = 'Aluno'; //Você pode criar atributos de forma dinâmica não necessariamente na declaração do objeto
usuario.senha = '123456';

console.log(usuario.email);
console.log(usuario.nome);
console.log(usuario);